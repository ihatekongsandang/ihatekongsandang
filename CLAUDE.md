# ihatekongsandang 프로젝트

> 멀티세션 협업 + md오더 시스템으로 운영한다(2026-09-21 사용자 결정). 세부 규칙은 본 문서와 `instructions/README.md`가 전부다.
> 🔴 **저장소 독립 원칙 (2026-09-21 사용자 지시 — 모든 담당자·모든 산출물)**: 이 사이트는 사용자의 다른 서비스와 **완전히 독립**이다.
> **모든 담당자의 산출물(오더·핸드오프·트리거·리뷰·history·코드·주석·커밋 메시지)에 다른 서비스의 이름·경로·URL·계정명, 사용자 홈 디렉터리명을 절대 쓰지 않는다.**
> 참고할 것이 있어도 내용만 가져오고 출처 프로젝트명은 쓰지 않는다. 절대경로는 `$HOME/ihatekongsandang/...` 또는 상대경로로 쓴다.
> 위반은 리뷰어 결함 확정 대상이며, 슈퍼바이저는 커밋 전 전수 grep으로 게이트한다(로컬 pre-commit 훅).

## ⚠️ 대전제 (모든 세션 절대 준수)

**품질 >>>>>>> 시간.** 속도·효율보다 **정확성·완전성이 압도적으로 우선**. 시간이 더 걸리는 게 맞다.
**이 3원칙은 다른 모든 지시에 우선한다.**

1. **추측·예상·갈음 절대 금지.** 모든 작업·답변·판정은 **실제 수행·실측·코드/빌드 직접 확인** 후에만.
   "아마"·"~일 것"·"샘플로 갈음" 금지. 근거 없으면 "확인 필요"로 정직하게 표기.
2. **직접 끝까지 수행 후 보고.** 시간·토큰이 많이 들어도 **무조건 직접 끝까지 수행** 후 보고.
   **전수 지시 = 전수**. 검사 수 = 전체 규모를 숫자로 증명.
3. **사전 안내.** 시간·토큰이 많이 소모될 것 같으면 **작업 전 미리 안내**.

## 기록 의무
모든 지시/행동/결과는 history/{YYYY-MM-DD}.md에 기록할 것.
형식: ## [HH:MM] 지시내용 → 행동 → 결과

---

## 프로젝트 개요

- **서비스명**: **공산당이싫어요** (2026-09-21 사용자 확정) · 슬러그 `ihatekongsandang` (폴더·레포·도메인)
- **소재** (🔴 2026-09-21 확장 확정): **정치·시사 이슈 큐레이션 전반** — 정책 비판·정치인 발언·논평·안보·간첩 행위·의혹. 간첩 이슈는 그중 하나.
  사용자 예시: 국회의원 인스타그램 릴스("집값은 못 잡고 국민만 잡아요", 전세 정책 비판) → 게시 대상.
  ⚠️ 이전 정의("국내 간첩 행위·의혹 한정")는 폐기. 게시물은 두 종류로 나뉜다:
  🔴 **2026-09-21 사용자 지시: 사건 상태 라벨 폐지.** 게시물 종류 구분(형사 사건/발언·논평)도 스키마에서 두지 않는다 — **단일 게시물 스키마**:
  카드 = 제목 + 이미지(사용자 원안 그대로), 상세 = 요약 + 원문 링크 + 출처 목록(+ 발언자 선택). 편집 정책은 문구·출처 규칙으로만 적용.
  PRD v0.6(기획자 06)에서 §3·§6 정리, 프로그래머 03이 코드 반영.
- **톤**: 정보 전달형 (유머·풍자·에세이 X)
- **독자**: 불특정 검색 유입 + 특정 관심사 커뮤니티 (→ SEO·공유 URL이 핵심 유입 경로)
- **운영자**: 익명 (`/about`에 실명·직함 노출 X)
- **레포**: https://github.com/ihatekongsandang/ihatekongsandang
- **한 줄**: 사용자가 슈퍼바이저에게 **URL / 사진 / 사진+글**을 건네면 그것이 **카드 형태 뉴스 소식지**로
  게시되는 사이트. 모바일·웹 모두 지원.
- **타깃 디바이스**: 모바일 우선 + 데스크톱 동시 지원.

### 확정된 결정 (2026-09-21 사용자)
| 항목 | 결정 |
|---|---|
| 게시 파이프라인 | **파일 기반** — 슈퍼바이저가 받은 URL·사진·글을 `content/posts/*.md`(+이미지)로 작성 → 커밋·푸시 → Vercel 자동 배포. DB·로그인·관리자 화면 없음 |
| URL 게시 | OG 메타(제목·썸네일·요약) 자동 추출 → 링크 카드 |
| MVP(v0.1) 범위 | **열람 전용 프로토타입** — 카드 피드 + 카테고리/태그 + 상세. 로그인·댓글·구독 없음 |
| 사용자 view 화면 (2026-09-21 확정) | **카드 = 제목 + 이미지**. 이미지 없는 게시물은 **빈 이미지(플레이스홀더)** 로 카드 형태 유지. **카드 클릭 → 상세 페이지 이동**해서 내용(링크 카드면 OG 요약·원문 링크, 사진/글이면 본문) 표시. 피드에서 본문·요약을 펼치지 않는다 |
| 이후 로드맵 | v0.1 게시물 프로토타입 → **v0.1.5 통계 연동(GA4·Google Search Console·네이버 서치어드바이저·sitemap 제출 — 디자인 전에 반드시 완료, 2026-09-21 사용자 지시)** → v0.2 디자인 수정 → **v0.3 제보받기**(방문자가 URL·사진·글을 제보 → 슈퍼바이저가 검수 후 게시) → v0.4 좋아요/싫어요 → v0.5 공유 기능 + 공유 숫자 표시 → v0.6 댓글 (2026-09-21 사용자 확정 순서) |
| 사용자 답변 (2026-09-21, user-questions 6~14) | **카테고리**: v0.1 생략, 태그만(사용자 "꼭 정해야 하나" → 슈퍼바이저 제안 채택, 게시물 축적 후 재검토) · **브랜드 비주얼 톤**: 담백하게 · **디바이스 대응**: 반응형 · **사진 저작권 확인**: 묻지 않음(사용자 책임 전제) · **도메인**: 추후 결정 · **게시 빈도·볼륨**: 미정 · **기존 콘텐츠**: 필요 시 다수 확보 가능 · **댓글 방식**: 추후 결정 · **피드 방식**: 무한스크롤+더보기 하이브리드 채택 |
| 사진만 받은 경우 (2026-09-21 확정) | 슈퍼바이저가 **사진 속 텍스트·장면을 직접 분석 → 관련 기사·짤·뉴스 웹 검색 → 허용 출처(§6②) 확인 → 초안 → 사용자 확인 → 게시**. 절차: `docs/supervisor/photo-post-workflow.md`. 허용 출처 0건이면 게시 불가·보고 |
| 후속 기능 백엔드 | 제보·좋아요·공유수·댓글은 서버 상태가 필요(제보는 Supabase 테이블 + Resend 이메일 알림 패턴 후보). **v0.1부터 게시물 안정 ID(슬러그)를 설계**해 이후 카운트·댓글 키로 쓴다 |
| 운영 체계 | md오더·핸드오프·트리거·리뷰어 게이트 |
| 슈퍼바이저 역할 | **사용자 커뮤니케이션 + 콘텐츠 게시(파일 작성) + 커밋·배포**. 그 외 모든 작업은 담당 세션에 md오더 |

### 기술 스택
| 항목 | 선택 |
|---|---|
| 프레임워크 | Next.js 15 (App Router, SSG) + React 19 + TypeScript |
| 스타일 | Tailwind + shadcn/ui |
| 콘텐츠 | `content/posts/*.md` (frontmatter) — 빌드 시 정적 생성 |
| 이미지 | `public/images/` 또는 Cloudflare R2 (규모 보고 프로그래머·리뷰어 판단) |
| 호스팅 | Vercel (GitHub push → 자동 배포) |
| 저장소 | GitHub (생성은 `docs/user-tasks.md` 추적) |

### 🔴 계정 분리 원칙 (2026-09-21 사용자 확정)
| 구분 | 계정 |
|---|---|
| **기능 계정** — Vercel · Supabase · GitHub 레포 · R2 · Resend 등 서비스 동작에 쓰는 것 | **이 프로젝트 전용 계정** (`ihatekongsandang`) |
| **통계 계정** — GA · Google Search Console · 네이버 서치어드바이저 등 | 사용자의 기존 통계 계정 (새 속성으로 분리) |

⚠️ 다른 프로젝트의 Supabase·R2·Resend 키를 **복사해 쓰지 않는다.** 이 프로젝트 전용 키만 사용.
GA 측정 ID는 **새 속성**으로 발급한다(다른 속성과 공유 X — 통계가 섞인다).
GitHub 레포 소유 계정도 기능 계정에 속하므로 사용자에게 확인 후 생성.

## 5대 핵심 축 (모든 기능에 적용)
1. UX/UI 친화성 — 모바일 우선, 진입 장벽 zero
2. SEO (Google·Naver·Daum)
3. AI 검색 최적화 (LLM 인용)
4. 웹표준·시맨틱·접근성 (WCAG 2.2 AA)
5. 보안 + **외부 콘텐츠 인용 시 저작권·출처 표기** (URL 카드는 OG 인용 범위 준수, 원문 전재 금지)
6. 🔴 **편집 정책 — 출처 기반·단정 금지** (본 프로젝트 특화, 2026-09-21 신설 · **상태 라벨 폐지 2026-09-21 사용자 지시**): 범죄 의혹 등 특정인을 향한 서술은
   명예훼손(사실적시 포함)·허위사실 리스크가 실재한다. 모든 게시물은 **출처(언론 보도·수사기관 발표·판결문 등) 기반**으로만,
   제목·요약은 **인용문 형태("~로 보도됨"·"~라는 의혹이 제기됨")** 로 쓰고 운영자 자체 단정 문구 금지.
   ⚠️ 카드·스키마의 **사건 상태 라벨(의혹/수사중/기소/유죄판결/종결)은 두지 않는다** — 사실 확인 단계는 요약 문장과 출처로만 전달.
   구체 규칙은 PRD(기획자)에서 정하고 리뷰어가 게이트한다.

---

## 세션 운영 프로토콜 (멀티세션 협업 필수)

### 1. 선행 문서 필수 검토 (작업 시작 전)
- 공통: `CLAUDE.md`, `PROJECT_STATUS.md`, `planning/prd.md`, `history/{최근일}.md`
- 디자이너/프로그래머: + `TECH_STACK.md`(작성 후), `design/backlog.md`, 이전 단계 핸드오프
- 리뷰어/테스터: + 모든 산출물 + 담당자 핸드오프 + 코드

### 2. 핸드오프 문서 작성 (작업 완료 시)
`docs/handoffs/{role}-{NN}-{task-slug}.md` + `docs/triggers/{role}-{NN}-{task-slug}-COMPLETE.md`

**표준 템플릿**:
```markdown
# {역할} 세션 — {NN}-{task-slug} 핸드오프
**작성일**: YYYY-MM-DD HH:MM | **작성 세션**: {역할} ({모델})

## 요약 (3줄)
## 산출물 (파일 경로 + 한 줄 설명 표)
## 주요 결정사항 (자체 판단 + 근거)
## 미해결·이슈 (다음 세션이 알아야 할 것)
## 다음 세션 가이드 (누가·무엇을·어떻게)
## 참고 링크
```

### 3. 슈퍼바이저(Opus 5) 검토 의무
다음 세션 지시 전 **반드시 이전 세션의 핸드오프 MD + 핵심 산출물을 직접 Read 검증** 후 지시. 보고만 받고 지시 금지.
🔴 슈퍼바이저는 코드·의존성·빌드·기획·디자인을 **직접 수정하지 않는다**. 전부 담당 세션 경유.
(예외: `content/posts/` 게시 파일 작성과 `history/`·`PROJECT_STATUS.md`·`docs/user-tasks.md`·md오더 — 슈퍼바이저 고유 영역)

### 4. 파일 경로 명시 의무
완료 보고 서두: `📋 작업 완료 보고 — {역할} ({모델}) · {NN}-{task-slug}` + 산출물 경로 + 핸드오프 경로 + 요약 3줄.

### 5. 🔴 리뷰어 게이트 — 커밋·배포 전 필수
- **한 묶음의 작업이 끝나면 무조건 리뷰어(Fable 5.1)를 거친다. 리뷰어 판정 전에 커밋·푸시하지 않는다.**
- 푸시 = 배포. 리뷰어를 건너뛴 배포 금지. 결함 확정 시 고친 뒤 커밋.
- 예외는 리뷰어 자신의 산출물뿐(그 경우 슈퍼바이저가 직접 Read 후 커밋).
- ⚠️ **콘텐츠 게시(`content/posts/`)는 예외** — 슈퍼바이저가 작성·커밋·배포한다. 단 게시 파일 형식은
  프로그래머가 정한 스키마(`content/README.md`)를 따르고, 빌드 검증 스크립트를 통과해야 한다.
- 리뷰 보고서: `docs/reviews/{role}-{NN}-{task-slug}-review.md`
- 리뷰어는 상시 가동 X — 세션 완료 시 슈퍼바이저가 호출. 호출 후 슈퍼바이저는 결과 대기.

| 검토 대상 | 리뷰어 중점 |
|---|---|
| 기획자 | 정책 충돌·우선순위·사용자 요청 누락·저작권 정책 |
| 디자이너 | 브랜드 일관성·접근성·모바일/데스크톱 차별화 |
| 프로그래머 | 코드 품질·보안·성능·빌드 검증·콘텐츠 스키마 견고성 |

### 6. 세션 모델 매핑 (2026-09-21)
| 세션 | 모델 | 활성 패턴 |
|---|---|---|
| 슈퍼바이저 | **Opus 5** | 상시 (커뮤니케이션·게시·커밋·배포) |
| 프로그래머 | **Opus 5** | 구현 시 |
| **리뷰어** | **Fable 5.1** | 각 세션 완료 시 간헐 호출 |
| 기획자 | Sonnet 5 | 기획 시 |
| 디자이너 | Sonnet 5 | 디자인 시 |
| 테스터 | Sonnet 5 | 구현 완료 후 |

⚠️ 오더·핸드오프·트리거의 괄호 표기는 이 값을 쓴다. 모델 표기는 사실 기록이므로 추정 금지.

### 7. md오더 시스템
상세: `instructions/README.md`. 요약 — `instructions/{role}/{NN}-{task-slug}.md` 작성 → 담당 세션 폴링 감지 →
실행 → 핸드오프·트리거(같은 NN) → `processed/` 이동. 사용자 직접 작성 금지(슈퍼바이저 경유).

### 8. 전수 측정 도구 저장·이월 재확인
- 전수 측정 결과 보고 시 측정 도구를 `docs/tools/{역할}-{번호}/`에 저장. 절대경로 금지.
- 미해결 항목 이월 전 해당 파일을 **다시 열어** 현재 상태 재확인.

### 9. 배포 도메인 접근 최소화
성능·화면 측정은 로컬 프로덕션 빌드(`npm run build && npx next start`). 배포 도메인은 반영 확인용 `curl` 1~2회만.

## 문서 구조
```
ihatekongsandang/
├── CLAUDE.md · PROJECT_STATUS.md · TECH_STACK.md(예정)
├── planning/prd.md
├── design/
├── content/posts/            ← 게시물 (슈퍼바이저 작성)
├── instructions/{role}/      ← md오더
├── docs/{handoffs,triggers,reviews,tools}/ · docs/user-tasks.md
└── history/YYYY-MM-DD.md
```
