# 폐기된 게시물 id 목록 — 안정 ID 원칙 3번(재사용 금지)
#
# 게시물을 삭제·비공개로 내릴 때, 파일을 지우고 그 id를 이 파일에 한 줄 추가한다.
# 여기 적힌 id를 새 게시물에 쓰면 `npm run validate:content`가 빌드를 막는다.
# (v0.4 좋아요·v0.5 공유수·v0.6 댓글이 id를 외래키로 쓰기 때문에,
#  id를 재사용하면 과거 데이터가 엉뚱한 게시물에 붙는다.)
#
# 형식: 한 줄에 id 하나. `#` 뒤는 주석.
example-case-a
example-case-b
example-case-c
